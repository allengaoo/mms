---
id: AD-003
layer: CC
module: cross_cutting
dimension: D2_architecture
type: decision
tier: hot
description: "services/control 只写 MySQL；向量/搜索写入必须通过 services/dispatch 发 Kafka 事件，禁止直接调用 Milvus"
tags: [cqrs, control-plane, data-plane, milvus, elasticsearch, kafka, architecture-boundary]
source_ep: EP-001
created_at: "2025-11-01"
version: 1
last_accessed: "2026-04-11"
access_count: 22
related_memories: []
generalized: false
related_to:
  - id: "AD-006"
    reason: "Worker 是数据面的核心执行者，JobExecutionScope 是数据面写操作的生命周期容器"
  - id: "AD-007"
    reason: "Kafka 是控制面→数据面的唯一通道，NullSafeNormalizer 是进入数据面前的强制门"
  - id: "MEM-L-016"
    reason: "数据管道 Worker 是数据面写操作的具体实现，必须遵守控制/数据面边界"
cites_files:
  - "backend/app/services/control/"
  - "backend/app/services/dispatch/"
  - "backend/app/workers/"
impacts:
  - "MEM-L-013"
  - "BIZ-002"
---

# AD-003 · 控制面 / 数据面严格分离（架构基线）

## 决策
`services/control`（MySQL）只管理元数据定义；写向量库/搜索引擎必须通过 Kafka 事件触发 Worker，禁止在 HTTP 请求链路中直接写 Milvus/ES。

## 背景
HTTP 请求链路不适合长耗时写操作（超时风险）；向量索引失败不应影响元数据写入事务。

## 影响
- 向量索引写入是最终一致性（eventual consistency）
- API 返回 `202 Accepted` 或客户端轮询状态
- ❌ 禁止：`services/control/` 中直接 `import pymilvus` 或 `import elasticsearch`
- ✅ 必须：通过 `services/dispatch/` 发 Kafka 事件，由 `workers/indexing.py` 消费
