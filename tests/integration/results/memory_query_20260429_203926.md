# mulan list / search / graph 集成测试报告（Sprint 4）

> 生成时间：2026-04-29 12:39:26 UTC　｜　覆盖命令：`mulan list` / `mulan search` / `mulan graph`

## 汇总

| 指标 | 值 |
|------|-----|
| 总用例数 | 25 |
| 通过 | 23 |
| 失败 | 2 |
| 总耗时 | 1.7s |
| 结果 | ❌ 2 个失败 |

## 详细结果

### mulan list 组（A 组）

#### ✅ [A-01] list --help 正常显示

**命令**：`mulan list --help`  
**期望 exit**：`0`　**实际 exit**：`0`

| 检查项 | 结果 | 期望值 |
|--------|------|--------|
| exit code 为 0 | ✅ | `0` |
| 含 --tier 选项 | ✅ | `--tier` |
| 含 --layer 选项 | ✅ | `--layer` |

**实际输出（前 500 字符）**
```
usage: mulan list [-h] [--tier {hot,warm,cold,archive}] [--layer L1-L5|CC]

optional arguments:
  -h, --help            show this help message and exit
  --tier {hot,warm,cold,archive}
                        按热度过滤
  --layer L1-L5|CC      按层过滤（如 L3）
```

#### ✅ [A-02] list 无参数运行 exit=0，含记忆条目

**命令**：`mulan list`  
**期望 exit**：`0`　**实际 exit**：`0`

| 检查项 | 结果 | 期望值 |
|--------|------|--------|
| exit code 为 0 | ✅ | `0` |
| 含 AC- 节点 ID | ✅ | `AC-XXX-N` |
| 含 HOT 或 WARM 层标签 | ✅ | `HOT/WARM` |

**实际输出（前 500 字符）**
```
=======================================================
  记忆列表
=======================================================

  CC     HOT      AC-ARCH-01         模块/包之间禁止循环依赖，架构依赖方向必须单向
  PLATFORM HOT      AC-ARCH-02         数据库迁移脚本中禁止包含业务逻辑
  PLATFORM HOT      AC-ARCH-03         配置值必须来自环境变量，禁止硬编码在代码中
  CC     HOT      AC-ARCH-04         日志输出禁止明文打印敏感信息
  PLATFORM HOT      AC-ARCH-05         所有外部服务调用必须配置超时和重试策略
  CC     WARM     AC-ARCH-06         生产代码中 TODO 注释超过 2 周必须转化为 Issue/Task
  PLATFORM HOT    
... (截断)
```

#### ✅ [A-03] list 输出含完整的 ID+标题 行

**命令**：`mulan list`  
**期望 exit**：`0`　**实际 exit**：`0`

| 检查项 | 结果 | 期望值 |
|--------|------|--------|
| 含 >20 个字符的 AC-* 行 | ✅ | `>0 行` |
| 条目数 > 50 | ✅ | `>50` |

**实际输出（前 500 字符）**
```
=======================================================
  记忆列表
=======================================================

  CC     HOT      AC-ARCH-01         模块/包之间禁止循环依赖，架构依赖方向必须单向
  PLATFORM HOT      AC-ARCH-02         数据库迁移脚本中禁止包含业务逻辑
  PLATFORM HOT      AC-ARCH-03         配置值必须来自环境变量，禁止硬编码在代码中
  CC     HOT      AC-ARCH-04         日志输出禁止明文打印敏感信息
  PLATFORM HOT      AC-ARCH-05         所有外部服务调用必须配置超时和重试策略
  CC     WARM     AC-ARCH-06         生产代码中 TODO 注释超过 2 周必须转化为 Issue/Task
  PLATFORM HOT    
... (截断)
```

#### ✅ [A-04] list --tier hot 只显示 HOT 层

**命令**：`mulan list --tier hot`  
**期望 exit**：`0`　**实际 exit**：`0`

| 检查项 | 结果 | 期望值 |
|--------|------|--------|
| exit code 为 0 | ✅ | `0` |
| 含 HOT | ✅ | `HOT` |
| 不含 WARM | ✅ | `不含 WARM` |
| 有 HOT 条目 | ✅ | `>0 条` |

**实际输出（前 500 字符）**
```
=======================================================
  记忆列表 [tier=hot]
=======================================================

  CC     HOT      AC-ARCH-01         模块/包之间禁止循环依赖，架构依赖方向必须单向
  PLATFORM HOT      AC-ARCH-02         数据库迁移脚本中禁止包含业务逻辑
  PLATFORM HOT      AC-ARCH-03         配置值必须来自环境变量，禁止硬编码在代码中
  CC     HOT      AC-ARCH-04         日志输出禁止明文打印敏感信息
  PLATFORM HOT      AC-ARCH-05         所有外部服务调用必须配置超时和重试策略
  PLATFORM HOT      AC-ARCH-07         消息队列消费者必须实现幂等性
  APP    HOT      AC-GO-01
... (截断)
```

#### ✅ [A-05] list --tier warm 只显示 WARM 层

**命令**：`mulan list --tier warm`  
**期望 exit**：`0`　**实际 exit**：`0`

| 检查项 | 结果 | 期望值 |
|--------|------|--------|
| exit code 为 0 | ✅ | `0` |
| 含 WARM | ✅ | `WARM` |
| 不含 HOT | ✅ | `不含 HOT` |

**实际输出（前 500 字符）**
```
=======================================================
  记忆列表 [tier=warm]
=======================================================

  CC     WARM     AC-ARCH-06         生产代码中 TODO 注释超过 2 周必须转化为 Issue/Task
  PLATFORM WARM     AC-ARCH-08         Outbox 模式：领域事件必须与业务写操作在同一事务中持久化
  DOMAIN WARM     AC-GO-04           禁止全局可变状态，必须通过构造函数依赖注入
  ADAPTER WARM     AC-GO-07           HTTP Handler 必须通过 context.WithTimeout 设置超时
  DOMAIN WARM     AC-GO-08           sync.Map 适合读多写少；写多读少用带 RWMutex 的普通 map
  DOMAIN
... (截断)
```

#### ✅ [A-06] list --layer CC 按层过滤

**命令**：`mulan list --layer CC`  
**期望 exit**：`0`　**实际 exit**：`0`

| 检查项 | 结果 | 期望值 |
|--------|------|--------|
| exit code 为 0 | ✅ | `0` |
| 含 CC 层标识 | ✅ | `CC` |

**实际输出（前 500 字符）**
```
=======================================================
  记忆列表 [layer=CC]
=======================================================

  CC     HOT      AC-ARCH-01         模块/包之间禁止循环依赖，架构依赖方向必须单向
  CC     HOT      AC-ARCH-04         日志输出禁止明文打印敏感信息
  CC     WARM     AC-ARCH-06         生产代码中 TODO 注释超过 2 周必须转化为 Issue/Task
  CC     HOT      AC-GO-02           错误必须用 fmt.Errorf("%w") 包裹，禁止裸返回原 err
  CC     HOT      AC-GO-03           所有执行 I/O 的函数第一个参数必须是 context.Context
  CC     WARM     AC-GO-10         
... (截断)
```

#### ✅ [A-07] list --tier hot --layer CC 组合过滤

**命令**：`mulan list --tier hot --layer CC`  
**期望 exit**：`0`　**实际 exit**：`0`

| 检查项 | 结果 | 期望值 |
|--------|------|--------|
| exit code 为 0 | ✅ | `0` |
| 不含 Traceback | ✅ | `无 Traceback` |

**实际输出（前 500 字符）**
```
=======================================================
  记忆列表 [tier=hot] [layer=CC]
=======================================================

  CC     HOT      AC-ARCH-01         模块/包之间禁止循环依赖，架构依赖方向必须单向
  CC     HOT      AC-ARCH-04         日志输出禁止明文打印敏感信息
  CC     HOT      AC-GO-02           错误必须用 fmt.Errorf("%w") 包裹，禁止裸返回原 err
  CC     HOT      AC-GO-03           所有执行 I/O 的函数第一个参数必须是 context.Context
  CC     HOT      AC-DEVOPS-01       AC-DEVOPS-01：Dockerfile 必须以非 root 用户运行
  CC     HOT      AC-
... (截断)
```

#### ✅ [A-08] list 运行完成 < 10 秒

**命令**：`mulan list（计时）`  
**期望 exit**：`0`　**实际 exit**：`0`

| 检查项 | 结果 | 期望值 |
|--------|------|--------|
| exit code 为 0 | ✅ | `0` |
| 耗时 0.06s < 10s | ✅ | `<10s` |

### mulan search 组（B 组）

#### ✅ [B-01] search --help 正常显示

**命令**：`mulan search --help`  
**期望 exit**：`0`　**实际 exit**：`0`

| 检查项 | 结果 | 期望值 |
|--------|------|--------|
| exit code 为 0 | ✅ | `0` |
| 含 keywords 位置参数 | ✅ | `keywords` |
| 含 --top-k 选项 | ✅ | `--top-k` |
| 含 --preview 选项 | ✅ | `--preview` |

**实际输出（前 500 字符）**
```
usage: mulan search [-h] [--top-k N] [--preview] keywords [keywords ...]

positional arguments:
  keywords    检索关键词（支持多个）

optional arguments:
  -h, --help  show this help message and exit
  --top-k N   返回条数（默认 5）
  --preview   预览最高匹配记忆的正文
```

#### ❌ [B-02] search 无关键词参数 exit=1 + 提示

**命令**：`mulan search（无关键词）`  
**期望 exit**：`1`　**实际 exit**：`2`

| 检查项 | 结果 | 期望值 |
|--------|------|--------|
| exit code 为 1 | ❌ | `1` |
| 含提示信息 | ❌ | `关键词` |

**stderr**
```
usage: mulan search [-h] [--top-k N] [--preview] keywords [keywords ...]
mulan search: error: the following arguments are required: keywords
```

#### ✅ [B-03] search 'python sqlalchemy' 返回结果

**命令**：`mulan search python sqlalchemy --top-k 5`  
**期望 exit**：`0`　**实际 exit**：`0`

| 检查项 | 结果 | 期望值 |
|--------|------|--------|
| exit code 为 0 | ✅ | `0` |
| 含 AC- 条目或 未找到提示 | ✅ | `AC-XXX/未找到` |
| 不含 Traceback | ✅ | `无 Traceback` |

**实际输出（前 500 字符）**
```
=======================================================
  关键词检索：python sqlalchemy
=======================================================

找到 5 条相关记忆（按匹配分数排序）：

  1. [HOT] AC-SQLALCH-03  score=6
     AC-SQLALCH-03：Session 必须通过 context manager 使用
     seed_packs/python_sqlalchemy/memories/AC-SQLALCH-03.md

  2. [HOT] AC-SQLALCH-02  score=6
     AC-SQLALCH-02：使用 select() 替代 session.query()
     seed_packs/python_sqlalchemy/memories/AC-SQLALCH-02.md

  3. [HOT] AC-SQLALCH-06  score=6
     AC-SQLALC
... (截断)
```

#### ✅ [B-04] search 不存在关键词 exit=0 + 未找到提示

**命令**：`mulan search xyzzy_nonexistent_keyword_42`  
**期望 exit**：`0`　**实际 exit**：`0`

| 检查项 | 结果 | 期望值 |
|--------|------|--------|
| exit code 为 0 | ✅ | `0` |
| 含 未找到 提示 | ✅ | `未找到` |

**实际输出（前 500 字符）**
```
=======================================================
  关键词检索：xyzzy_nonexistent_keyword_42
=======================================================
  ⚠️  未找到匹配的记忆。建议扩展关键词或检查 MEMORY_INDEX.json 中的 trigger_keywords。
```

#### ❌ [B-05] search --top-k 3 返回不超过 3 条

**命令**：`mulan search python --top-k 3`  
**期望 exit**：`0`　**实际 exit**：`0`

| 检查项 | 结果 | 期望值 |
|--------|------|--------|
| exit code 为 0 | ✅ | `0` |
| 结果数 ≤ 3（实际 9） | ❌ | `≤3` |

**实际输出（前 500 字符）**
```
=======================================================
  关键词检索：python
=======================================================

找到 3 条相关记忆（按匹配分数排序）：

  1. [HOT] AC-SQLALCH-03  score=3
     AC-SQLALCH-03：Session 必须通过 context manager 使用
     seed_packs/python_sqlalchemy/memories/AC-SQLALCH-03.md

  2. [HOT] AC-SQLALCH-02  score=3
     AC-SQLALCH-02：使用 select() 替代 session.query()
     seed_packs/python_sqlalchemy/memories/AC-SQLALCH-02.md

  3. [HOT] AC-SQLALCH-06  score=3
     AC-SQLALCH-06：异步场景必须
... (截断)
```

#### ✅ [B-06] search --preview 含文件正文预览

**命令**：`mulan search python --preview --top-k 1`  
**期望 exit**：`0`　**实际 exit**：`0`

| 检查项 | 结果 | 期望值 |
|--------|------|--------|
| exit code 为 0 | ✅ | `0` |
| 不含 Traceback | ✅ | `无 Traceback` |
| 若有结果则含 预览 关键词 | ✅ | `预览/未找到/AC-*` |

**实际输出（前 500 字符）**
```
=======================================================
  关键词检索：python
=======================================================

找到 1 条相关记忆（按匹配分数排序）：

  1. [HOT] AC-SQLALCH-03  score=3
     AC-SQLALCH-03：Session 必须通过 context manager 使用
     seed_packs/python_sqlalchemy/memories/AC-SQLALCH-03.md


── 最高匹配记忆预览 ──
# AC-SQLALCH-03：Session 必须通过 context manager 使用

## 约束
MUST 使用 `with Session(engine) as session` 或 `with session_factory() as session`；
NEVER 在 finally 块中手动调用 `session.close()`；Session 应在 
... (截断)
```

#### ✅ [B-07] search 多关键词联合搜索不崩溃

**命令**：`mulan search redis cache timeout --top-k 5`  
**期望 exit**：`0`　**实际 exit**：`0`

| 检查项 | 结果 | 期望值 |
|--------|------|--------|
| exit code 为 0 | ✅ | `0` |
| 不含 Traceback | ✅ | `无 Traceback` |

**实际输出（前 500 字符）**
```
=======================================================
  关键词检索：redis cache timeout
=======================================================

找到 5 条相关记忆（按匹配分数排序）：

  1. [HOT] AC-REDIS-01  score=2
     AC-REDIS-01：所有缓存写入必须设置 TTL
     seed_packs/infrastructure_redis/memories/AC-REDIS-01.md

  2. [HOT] AC-REDIS-04  score=1
     AC-REDIS-04：禁止在生产环境使用 KEYS * 命令
     seed_packs/infrastructure_redis/memories/AC-REDIS-04.md

  3. [WARM] AC-REDIS-05  score=1
     AC-REDIS-05：异步场景必须使用 redis.asyncio
     se
... (截断)
```

#### ✅ [B-08] search 'goroutine context' 命中 Go 种子包

**命令**：`mulan search goroutine context`  
**期望 exit**：`0`　**实际 exit**：`0`

| 检查项 | 结果 | 期望值 |
|--------|------|--------|
| exit code 为 0 | ✅ | `0` |
| 含 AC-GO 或 未找到 | ✅ | `AC-GO/未找到` |

**实际输出（前 500 字符）**
```
=======================================================
  关键词检索：goroutine context
=======================================================
  ⚠️  未找到匹配的记忆。建议扩展关键词或检查 MEMORY_INDEX.json 中的 trigger_keywords。
```

### mulan graph 组（C 组）

#### ✅ [C-01] graph stats exit=0，含节点总数

**命令**：`mulan graph stats`  
**期望 exit**：`0`　**实际 exit**：`0`

| 检查项 | 结果 | 期望值 |
|--------|------|--------|
| exit code 为 0 | ✅ | `0` |
| 含 节点 统计 | ✅ | `节点` |
| 含 Hot 层 统计 | ✅ | `Hot` |
| 节点数 > 0 | ✅ | `总节点数：N>0` |

**实际输出（前 500 字符）**
```
[1m记忆知识图谱统计[0m
  总节点数：67
  总图边数：0
  有文件引用的节点：50
  Hot 层节点：35
  Warm 层节点：32
  Cold 层节点：0
```

#### ✅ [C-02] graph stats 包含图边数统计（当前为 0）

**命令**：`mulan graph stats`  
**期望 exit**：`0`　**实际 exit**：`0`

| 检查项 | 结果 | 期望值 |
|--------|------|--------|
| 含 总图边数 字段 | ✅ | `总图边数` |

**实际输出（前 500 字符）**
```
[1m记忆知识图谱统计[0m
  总节点数：67
  总图边数：0
  有文件引用的节点：50
  Hot 层节点：35
  Warm 层节点：32
  Cold 层节点：0
```

#### ✅ [C-03] graph explore 有效节点（无 related_to 时返回空）

**命令**：`mulan graph explore AC-GO-01`  
**期望 exit**：`1`　**实际 exit**：`1`

| 检查项 | 结果 | 期望值 |
|--------|------|--------|
| exit code 在 [0,1] | ✅ | `0/1` |
| 不含 Traceback | ✅ | `无 Traceback` |
| 含节点相关提示或 未找到 | ✅ | `AC-GO-01/未找到/节点` |

**实际输出（前 500 字符）**
```
⚠️  未找到节点：AC-GO-01
```

#### ✅ [C-04] graph explore 不存在节点 exit=1 + 友好提示

**命令**：`mulan graph explore NONEXISTENT-ID-99999`  
**期望 exit**：`1`　**实际 exit**：`1`

| 检查项 | 结果 | 期望值 |
|--------|------|--------|
| exit code 为 1 | ✅ | `1` |
| 含 未找到节点 提示 | ✅ | `未找到` |

**实际输出（前 500 字符）**
```
⚠️  未找到节点：NONEXISTENT-ID-99999
```

#### ✅ [C-05] graph file 引用文件反查（有结果或友好空结果）

**命令**：`mulan graph file seed_packs/go_microservice/memories/AC-GO-01.md`  
**期望 exit**：`0`　**实际 exit**：`0`

| 检查项 | 结果 | 期望值 |
|--------|------|--------|
| exit code 在 [0,1] | ✅ | `0/1` |
| 不含 Traceback | ✅ | `无 Traceback` |
| 含引用数量或无节点提示 | ✅ | `共N/没有/引用` |

**实际输出（前 500 字符）**
```
⚠️  没有记忆节点引用文件：seed_packs/go_microservice/memories/AC-GO-01.md
```

#### ✅ [C-06] graph file 不存在路径 exit=0 + 友好提示

**命令**：`mulan graph file nonexistent/path/file.py`  
**期望 exit**：`0`　**实际 exit**：`0`

| 检查项 | 结果 | 期望值 |
|--------|------|--------|
| exit code 为 0 | ✅ | `0` |
| 含 没有 或 未引用 | ✅ | `没有/0 个` |

**实际输出（前 500 字符）**
```
⚠️  没有记忆节点引用文件：nonexistent/path/file.py
```

#### ✅ [C-07] graph impacts 有效节点 exit=0，含影响统计

**命令**：`mulan graph impacts AC-GO-01`  
**期望 exit**：`0`　**实际 exit**：`0`

| 检查项 | 结果 | 期望值 |
|--------|------|--------|
| exit code 为 0 | ✅ | `0` |
| 含 变更 或 同步 | ✅ | `变更/同步` |
| 含 共 N 个 格式 | ✅ | `共N个` |

**实际输出（前 500 字符）**
```
[1mAC-GO-01 变更时需同步检查的节点（共 0 个）:[0m
```

#### ✅ [C-08] graph impacts 不存在节点 exit=0，0 个影响

**命令**：`mulan graph impacts NONEXISTENT-ID-99999`  
**期望 exit**：`0`　**实际 exit**：`0`

| 检查项 | 结果 | 期望值 |
|--------|------|--------|
| exit code 为 0 | ✅ | `0` |
| 含 0 个 影响节点 | ✅ | `共 0 个` |

**实际输出（前 500 字符）**
```
[1mNONEXISTENT-ID-99999 变更时需同步检查的节点（共 0 个）:[0m
```

#### ✅ [C-09] graph --help 显示所有子命令

**命令**：`mulan graph --help`  
**期望 exit**：`0`　**实际 exit**：`0`

| 检查项 | 结果 | 期望值 |
|--------|------|--------|
| exit code 为 0 | ✅ | `0` |
| 含 stats 子命令 | ✅ | `stats` |
| 含 explore 子命令 | ✅ | `explore` |
| 含 file 子命令 | ✅ | `file` |
| 含 impacts 子命令 | ✅ | `impacts` |

**实际输出（前 500 字符）**
```
usage: mulan graph [-h] <subcommand> ...

positional arguments:
  <subcommand>
    stats       图谱统计（节点数、边数、孤立节点）
    explore     从指定节点出发做 BFS 图遍历
    file        反查引用某文件的所有记忆节点
    impacts     查询某记忆变更时需同步检查的节点

optional arguments:
  -h, --help    show this help message and exit
```
