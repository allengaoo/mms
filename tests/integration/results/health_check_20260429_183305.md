# mulan 系统健康检查集成测试报告

> 生成时间：2026-04-29 10:33:05 UTC　｜　覆盖命令：`mulan status` / `mulan verify` / `mulan validate`

## 汇总

| 指标 | 值 |
|------|-----|
| 总用例数 | 22 |
| 通过 | 20 |
| 失败 | 2 |
| 总耗时 | 2.4s |
| 结果 | ❌ 2 个失败 |

## 详细结果

### mulan status 组（A 组）

#### ✅ [A-01] status 无参数运行正常（exit=0）

**命令**：`mulan status`  
**期望 exit**：`0`　**实际 exit**：`0`

| 检查项 | 结果 | 期望值 |
|--------|------|--------|
| exit code 为 0 | ✅ | `0` |
| 含 MMS 系统状态 标题 | ✅ | `MMS 系统状态` |
| 含 熔断器状态 区块 | ✅ | `熔断器状态` |
| 含 记忆库统计 区块 | ✅ | `记忆库统计` |

**实际输出（前 600 字符）**
```
=======================================================
  MMS 系统状态
=======================================================

【百炼（DashScope）服务】
  ⚠️  DASHSCOPE_API_KEY 未配置 — 百炼服务不可用
  ⚠️  请在 .env.memory 中设置：DASHSCOPE_API_KEY=sk-...

【熔断器状态】
  ✅ 熔断器状态文件未初始化（正常）

【记忆库统计】
  总计 69 条  |  HOT: 36  WARM: 33
  · 按层分布：ADAPTER: 9  APP: 6  CC: 18  DOMAIN: 15  L2: 7  PLATFORM: 13
  ✅ 无未完成的蒸馏断点

【模型使用统计（近 7 天）】
  · 暂无调用记录（首次调用 mms inject / mulan distill 后自动记录）
  · 运行 `mulan usage` 查看详细统计

【记忆图健康（Memory Graph Health）】
记忆图健康：
  节点总数：69  热节点：36  温节点：33  冷节点：0  归档：0
  有 cites 边：50/69 (72%)
  有 about 边：50/69 (72%
... (截断)
```

#### ✅ [A-02] status 输出包含记忆图健康区块

**命令**：`mulan status`  
**期望 exit**：`0`　**实际 exit**：`0`

| 检查项 | 结果 | 期望值 |
|--------|------|--------|
| 含 图健康 区块标题 | ✅ | `Memory Graph Health` |
| 含 节点总数 | ✅ | `节点总数` |
| 含 图密度 | ✅ | `图密度` |

**实际输出（前 600 字符）**
```
=======================================================
  MMS 系统状态
=======================================================

【百炼（DashScope）服务】
  ⚠️  DASHSCOPE_API_KEY 未配置 — 百炼服务不可用
  ⚠️  请在 .env.memory 中设置：DASHSCOPE_API_KEY=sk-...

【熔断器状态】
  ✅ 熔断器状态文件未初始化（正常）

【记忆库统计】
  总计 69 条  |  HOT: 36  WARM: 33
  · 按层分布：ADAPTER: 9  APP: 6  CC: 18  DOMAIN: 15  L2: 7  PLATFORM: 13
  ✅ 无未完成的蒸馏断点

【模型使用统计（近 7 天）】
  · 暂无调用记录（首次调用 mms inject / mulan distill 后自动记录）
  · 运行 `mulan usage` 查看详细统计

【记忆图健康（Memory Graph Health）】
记忆图健康：
  节点总数：69  热节点：36  温节点：33  冷节点：0  归档：0
  有 cites 边：50/69 (72%)
  有 about 边：50/69 (72%
... (截断)
```

#### ✅ [A-03] status 输出含 tier 分布统计

**命令**：`mulan status`  
**期望 exit**：`0`　**实际 exit**：`0`

| 检查项 | 结果 | 期望值 |
|--------|------|--------|
| 含 HOT 或 WARM 关键词 | ✅ | `HOT/WARM` |
| 含 总计 N 条 | ✅ | `总计 N 条` |
| 含 按层分布 | ✅ | `按层分布` |

**实际输出（前 600 字符）**
```
=======================================================
  MMS 系统状态
=======================================================

【百炼（DashScope）服务】
  ⚠️  DASHSCOPE_API_KEY 未配置 — 百炼服务不可用
  ⚠️  请在 .env.memory 中设置：DASHSCOPE_API_KEY=sk-...

【熔断器状态】
  ✅ 熔断器状态文件未初始化（正常）

【记忆库统计】
  总计 69 条  |  HOT: 36  WARM: 33
  · 按层分布：ADAPTER: 9  APP: 6  CC: 18  DOMAIN: 15  L2: 7  PLATFORM: 13
  ✅ 无未完成的蒸馏断点

【模型使用统计（近 7 天）】
  · 暂无调用记录（首次调用 mms inject / mulan distill 后自动记录）
  · 运行 `mulan usage` 查看详细统计

【记忆图健康（Memory Graph Health）】
记忆图健康：
  节点总数：69  热节点：36  温节点：33  冷节点：0  归档：0
  有 cites 边：50/69 (72%)
  有 about 边：50/69 (72%
... (截断)
```

#### ✅ [A-04] status 正确感知 DASHSCOPE_API_KEY 配置状态

**命令**：`mulan status`  
**期望 exit**：`0`　**实际 exit**：`0`

| 检查项 | 结果 | 期望值 |
|--------|------|--------|
| DASHSCOPE 未配置时含警告 ⚠️ | ✅ | `DASHSCOPE_API_KEY / 百炼` |
| 含 百炼 区块 | ✅ | `百炼` |

**实际输出（前 600 字符）**
```
=======================================================
  MMS 系统状态
=======================================================

【百炼（DashScope）服务】
  ⚠️  DASHSCOPE_API_KEY 未配置 — 百炼服务不可用
  ⚠️  请在 .env.memory 中设置：DASHSCOPE_API_KEY=sk-...

【熔断器状态】
  ✅ 熔断器状态文件未初始化（正常）

【记忆库统计】
  总计 69 条  |  HOT: 36  WARM: 33
  · 按层分布：ADAPTER: 9  APP: 6  CC: 18  DOMAIN: 15  L2: 7  PLATFORM: 13
  ✅ 无未完成的蒸馏断点

【模型使用统计（近 7 天）】
  · 暂无调用记录（首次调用 mms inject / mulan distill 后自动记录）
  · 运行 `mulan usage` 查看详细统计

【记忆图健康（Memory Graph Health）】
记忆图健康：
  节点总数：69  热节点：36  温节点：33  冷节点：0  归档：0
  有 cites 边：50/69 (72%)
  有 about 边：50/69 (72%
... (截断)
```

#### ✅ [A-05] status 熔断器状态显示正常或已开启

**命令**：`mulan status`  
**期望 exit**：`0`　**实际 exit**：`0`

| 检查项 | 结果 | 期望值 |
|--------|------|--------|
| 含熔断器相关描述 | ✅ | `熔断器 + 状态描述` |

**实际输出（前 600 字符）**
```
=======================================================
  MMS 系统状态
=======================================================

【百炼（DashScope）服务】
  ⚠️  DASHSCOPE_API_KEY 未配置 — 百炼服务不可用
  ⚠️  请在 .env.memory 中设置：DASHSCOPE_API_KEY=sk-...

【熔断器状态】
  ✅ 熔断器状态文件未初始化（正常）

【记忆库统计】
  总计 69 条  |  HOT: 36  WARM: 33
  · 按层分布：ADAPTER: 9  APP: 6  CC: 18  DOMAIN: 15  L2: 7  PLATFORM: 13
  ✅ 无未完成的蒸馏断点

【模型使用统计（近 7 天）】
  · 暂无调用记录（首次调用 mms inject / mulan distill 后自动记录）
  · 运行 `mulan usage` 查看详细统计

【记忆图健康（Memory Graph Health）】
记忆图健康：
  节点总数：69  热节点：36  温节点：33  冷节点：0  归档：0
  有 cites 边：50/69 (72%)
  有 about 边：50/69 (72%
... (截断)
```

#### ✅ [A-06] status --help 正常显示

**命令**：`mulan status --help`  
**期望 exit**：`0`　**实际 exit**：`0`

| 检查项 | 结果 | 期望值 |
|--------|------|--------|
| exit code 为 0 | ✅ | `0` |
| 含 usage: | ✅ | `usage:` |

**实际输出（前 600 字符）**
```
usage: mulan status [-h]

optional arguments:
  -h, --help  show this help message and exit
```

#### ✅ [A-07] status 执行完成 < 5 秒

**命令**：`mulan status（计时）`  
**期望 exit**：`0`　**实际 exit**：`0`

| 检查项 | 结果 | 期望值 |
|--------|------|--------|
| exit code 为 0 | ✅ | `0` |
| 耗时 0.13s < 5s | ✅ | `<5s` |

**实际输出（前 600 字符）**
```
=======================================================
  MMS 系统状态
=======================================================

【百炼（DashScope）服务】
  ⚠️  DASHSCOPE_API_KEY 未配置 — 百炼服务不可用
  ⚠️  请在 .env.memory 中设置：DASHSCOPE_API_KEY=sk-...

【熔断器状态】
  ✅ 熔断器状态文件未初始化（正常）

【记忆库统计】
  总计 69 条  |  HOT: 36  WARM: 33
  · 按层分布：ADAPTER: 9  APP: 6  CC: 18  DOMAIN: 15  L2: 7  PLATFORM: 13
  ✅ 无未完成的蒸馏断点

【模型使用统计（近 7 天）】
  · 暂无调用记录（首次调用 mms inject / mulan distill 后自动记录）
  · 运行 `mulan usage` 查看详细统计

【记忆图健康（Memory Graph Health）】
记忆图健康：
  节点总数：69  热节点：36  温节点：33  冷节点：0  归档：0
  有 cites 边：50/69 (72%)
  有 about 边：50/69 (72%
... (截断)
```

### mulan verify 组（B 组）

#### ✅ [B-01] verify 无参数运行不崩溃（exit 0 或 1）

**命令**：`mulan verify`  
**期望 exit**：`1`　**实际 exit**：`1`

| 检查项 | 结果 | 期望值 |
|--------|------|--------|
| exit code 在合法范围 [0,1,2] | ✅ | `[0,1,2]` |
| 含 MMS 健康检查 标题 | ✅ | `MMS 健康检查` |
| 含 4 个检查项标题之一 | ✅ | `Schema/索引/文档/前端` |

**实际输出（前 600 字符）**
```
MMS 健康检查
──────────────────────────────────────────────────

▶ Schema 校验
  ✅ 通过

▶ 索引一致性
  ❌ [index] MEMORY_INDEX.json 不存在

▶ 文档漂移检测
  ⚠️  [docs] e2e_traceability.md 不存在
  ⚠️  [docs] frontend_page_map.md 不存在

▶ 前端规范检查
  ✅ 通过

──────────────────────────────────────────────────
  ❌ 1 个错误，2 个警告
```

#### ✅ [B-02] verify --schema 只检查 Schema 项

**命令**：`mulan verify --schema`  
**期望 exit**：`0`　**实际 exit**：`0`

| 检查项 | 结果 | 期望值 |
|--------|------|--------|
| exit code 为 0 | ✅ | `0` |
| 输出含 Schema 校验 区块 | ✅ | `Schema` |
| 通过提示 ✅ | ✅ | `✅` |
| 不含 索引一致性 区块 | ✅ | `不含 索引一致性` |

**实际输出（前 600 字符）**
```
MMS 健康检查
──────────────────────────────────────────────────

▶ Schema 校验
  ✅ 通过

──────────────────────────────────────────────────
  ✅ 全部检查通过
```

#### ✅ [B-03] verify --index 正确反映索引状态

**命令**：`mulan verify --index`  
**期望 exit**：`1`　**实际 exit**：`1`

| 检查项 | 结果 | 期望值 |
|--------|------|--------|
| 索引缺失时 exit=1 且含 ❌ + MEMORY_INDEX | ✅ | `❌ MEMORY_INDEX / ✅` |
| 含 索引一致性 关键词 | ✅ | `索引` |

**实际输出（前 600 字符）**
```
MMS 健康检查
──────────────────────────────────────────────────

▶ 索引一致性
  ❌ [index] MEMORY_INDEX.json 不存在

──────────────────────────────────────────────────
  ❌ 1 个错误，0 个警告
```

#### ✅ [B-04] verify --docs 执行文档漂移检测

**命令**：`mulan verify --docs`  
**期望 exit**：`0`　**实际 exit**：`0`

| 检查项 | 结果 | 期望值 |
|--------|------|--------|
| exit code 在 [0,1] | ✅ | `[0,1]` |
| 含 docs 或 文档 关键词 | ✅ | `docs/文档` |

**实际输出（前 600 字符）**
```
MMS 健康检查
──────────────────────────────────────────────────

▶ 文档漂移检测
  ⚠️  [docs] e2e_traceability.md 不存在
  ⚠️  [docs] frontend_page_map.md 不存在

──────────────────────────────────────────────────
  ⚠️  0 个错误，2 个警告（不阻断 CI）
```

#### ✅ [B-05] verify --frontend 独立运行

**命令**：`mulan verify --frontend`  
**期望 exit**：`0`　**实际 exit**：`0`

| 检查项 | 结果 | 期望值 |
|--------|------|--------|
| exit code 为 0 | ✅ | `0` |
| 含 前端 或 ✅ | ✅ | `前端/✅` |

**实际输出（前 600 字符）**
```
MMS 健康检查
──────────────────────────────────────────────────

▶ 前端规范检查
  ✅ 通过

──────────────────────────────────────────────────
  ✅ 全部检查通过
```

#### ✅ [B-06] verify --ci 有错误时 exit=2（CI 模式）

**命令**：`mulan verify --ci`  
**期望 exit**：`2`　**实际 exit**：`2`

| 检查项 | 结果 | 期望值 |
|--------|------|--------|
| --ci 有错误时 exit=2 | ✅ | `2` |
| 含错误汇总行 | ✅ | `N 个错误` |

**实际输出（前 600 字符）**
```
MMS 健康检查
──────────────────────────────────────────────────

▶ Schema 校验
  ✅ 通过

▶ 索引一致性
  ❌ [index] MEMORY_INDEX.json 不存在

▶ 文档漂移检测
  ⚠️  [docs] e2e_traceability.md 不存在
  ⚠️  [docs] frontend_page_map.md 不存在

▶ 前端规范检查
  ✅ 通过

──────────────────────────────────────────────────
  ❌ 1 个错误，2 个警告
```

#### ✅ [B-07] verify --help 正常显示所有子选项

**命令**：`mulan verify --help`  
**期望 exit**：`0`　**实际 exit**：`0`

| 检查项 | 结果 | 期望值 |
|--------|------|--------|
| exit code 为 0 | ✅ | `0` |
| 含 --schema 选项 | ✅ | `--schema` |
| 含 --index 选项 | ✅ | `--index` |
| 含 --docs 选项 | ✅ | `--docs` |
| 含 --ci 选项 | ✅ | `--ci` |

**实际输出（前 600 字符）**
```
usage: mulan verify [-h] [--schema] [--index] [--docs] [--frontend] [--ci]

optional arguments:
  -h, --help  show this help message and exit
  --schema    只检查 YAML front-matter
  --index     只检查索引一致性
  --docs      只检查文档漂移
  --frontend  只检查前端规范
  --ci        CI 模式（错误则 exit 2）
```

#### ✅ [B-08] verify 与 status 输出职责不重叠

**命令**：`mulan status / mulan verify（对比）`  
**期望 exit**：`0`　**实际 exit**：`0`

| 检查项 | 结果 | 期望值 |
|--------|------|--------|
| verify 输出不含 status 专有词「记忆库统计」 | ✅ | `不含 记忆库统计` |
| status 输出不含 verify 专有词「Schema 校验」 | ✅ | `不含 Schema 校验` |

**实际输出（前 600 字符）**
```
MMS 健康检查
──────────────────────────────────────────────────

▶ Schema 校验
  ✅ 通过

▶ 索引一致性
  ❌ [index] MEMORY_INDEX.json 不存在

▶ 文档漂移检测
  ⚠️  [docs] e2e_traceability.md 不存在
  ⚠️  [docs] frontend_page_map.md 不存在

▶ 前端规范检查
  ✅ 通过

──────────────────────────────────────────────────
  ❌ 1 个错误，2 个警告
```

### mulan validate 组（C 组）

#### ✅ [C-01] validate --help 正常显示

**命令**：`mulan validate --help`  
**期望 exit**：`0`　**实际 exit**：`0`

| 检查项 | 结果 | 期望值 |
|--------|------|--------|
| exit code 为 0 | ✅ | `0` |
| 含 --changed-only 选项 | ✅ | `--changed-only` |
| 含 --file 选项 | ✅ | `--file` |
| 含 --migrate-add-version 选项 | ✅ | `--migrate-add-version` |

**实际输出（前 600 字符）**
```
usage: mulan validate [-h] [--changed-only] [--file NAME]
                      [--migrate-add-version]

optional arguments:
  -h, --help            show this help message and exit
  --changed-only        只校验 git diff 变更文件
  --file NAME           校验单个文件（ID 或部分文件名）
  --migrate-add-version
                        批量添加 version: 1 字段
```

#### ❌ [C-02] validate --file 对合法 frontmatter 文件 exit=0

**命令**：`mulan validate --file __ci_test__valid_memory`  
**期望 exit**：`0`　**实际 exit**：`2`

| 检查项 | 结果 | 期望值 |
|--------|------|--------|
| exit code 为 0 | ❌ | `0` |
| 输出含 ✅ | ❌ | `✅` |
| 不含 ❌ | ❌ | `不含 ❌` |

**实际输出（前 600 字符）**
```
❌ 未找到文件: __ci_test__valid_memory
```

#### ❌ [C-03] validate --file 对缺 frontmatter 文件 exit=1 + ❌

**命令**：`mulan validate --file __ci_test__bad_memory`  
**期望 exit**：`1`　**实际 exit**：`2`

| 检查项 | 结果 | 期望值 |
|--------|------|--------|
| exit code 为 1 | ❌ | `1` |
| 输出含 ❌ | ✅ | `❌` |
| 含 frontmatter 关键词 | ❌ | `front-matter` |

**实际输出（前 600 字符）**
```
❌ 未找到文件: __ci_test__bad_memory
```

#### ✅ [C-04] validate --file 不存在的文件 exit=2 + 友好提示

**命令**：`mulan validate --file __ci_nonexistent_xxxx__`  
**期望 exit**：`2`　**实际 exit**：`2`

| 检查项 | 结果 | 期望值 |
|--------|------|--------|
| exit code 为 2 | ✅ | `2` |
| 含 未找到文件 提示 | ✅ | `未找到` |

**实际输出（前 600 字符）**
```
❌ 未找到文件: __ci_nonexistent_xxxx__
```

#### ✅ [C-05] validate --changed-only（可能无变更文件时正常退出）

**命令**：`mulan validate --changed-only`  
**期望 exit**：`0`　**实际 exit**：`0`

| 检查项 | 结果 | 期望值 |
|--------|------|--------|
| exit code 在 [0,1] | ✅ | `[0,1]` |
| 不崩溃（无异常输出） | ✅ | `无 Traceback/Error` |

**实际输出（前 600 字符）**
```
✅ 无变更的记忆文件，校验跳过
```

#### ✅ [C-06] validate 全量扫描完成 < 10 秒

**命令**：`mulan validate（计时）`  
**期望 exit**：`1`　**实际 exit**：`1`

| 检查项 | 结果 | 期望值 |
|--------|------|--------|
| 耗时 0.13s < 10s | ✅ | `<10s` |
| 不崩溃 | ✅ | `无 Traceback` |
| 输出含 校验完成 汇总行 | ✅ | `校验完成` |

**实际输出（前 600 字符）**
```
🔍 校验全部 1 个记忆文件...
❌ BIZ-memory.md:
   · id 格式错误: 'BIZ-XXX                        # 唯一 ID，格式 BIZ-NNN'（应匹配 MEM-L/MEM-E/AD/PAT/SKL-xxx）
   · layer 值非法: 'BIZ                         # 固定为 BIZ（业务逻辑维度）'（合法值: ['BIZ', 'CC', 'L1', 'L1_platform', 'L2', 'L2_infrastructure', 'L3', 'L3_domain', 'L4', 'L4_application', 'L5', 'L5_interface', 'cross_cutting']）
   · type 值非法: 'business-flow                # business-flow | actor-model | constraint | edge-case'（合法值: ['actor-model', 'business-flow', 'constraint', 'decision', 'edge-case', 'error', 'lesson', 'pattern', 'skill']）
   · tier 值非法: 'hot                
... (截断)
```

#### ✅ [C-07] validate 输出含合法的通过/失败计数格式

**命令**：`mulan validate`  
**期望 exit**：`1`　**实际 exit**：`1`

| 检查项 | 结果 | 期望值 |
|--------|------|--------|
| 含 校验完成 字样 | ✅ | `校验完成` |
| 含 通过 和 失败 字样 | ✅ | `通过 + 失败` |
| 含数字统计 | ✅ | `N 通过` |

**实际输出（前 600 字符）**
```
🔍 校验全部 1 个记忆文件...
❌ BIZ-memory.md:
   · id 格式错误: 'BIZ-XXX                        # 唯一 ID，格式 BIZ-NNN'（应匹配 MEM-L/MEM-E/AD/PAT/SKL-xxx）
   · layer 值非法: 'BIZ                         # 固定为 BIZ（业务逻辑维度）'（合法值: ['BIZ', 'CC', 'L1', 'L1_platform', 'L2', 'L2_infrastructure', 'L3', 'L3_domain', 'L4', 'L4_application', 'L5', 'L5_interface', 'cross_cutting']）
   · type 值非法: 'business-flow                # business-flow | actor-model | constraint | edge-case'（合法值: ['actor-model', 'business-flow', 'constraint', 'decision', 'edge-case', 'error', 'lesson', 'pattern', 'skill']）
   · tier 值非法: 'hot                
... (截断)
```
