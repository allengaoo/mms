# mulan codemap / funcmap / ast-diff 集成测试报告

> 生成时间：2026-04-29 11:54:11 UTC　｜　覆盖命令：`mulan codemap` / `mulan funcmap` / `mulan ast-diff`

## 汇总

| 指标 | 值 |
|------|-----|
| 总用例数 | 22 |
| 通过 | 22 |
| 失败 | 0 |
| 总耗时 | 1.2s |
| 结果 | ✅ 全部通过 |

## 详细结果

### mulan codemap 组（A 组）

#### ✅ [A-01] codemap --help 正常显示

**命令**：`mulan codemap --help`  
**期望 exit**：`0`　**实际 exit**：`0`

| 检查项 | 结果 | 期望值 |
|--------|------|--------|
| exit code 为 0 | ✅ | `0` |
| 含 --depth 选项 | ✅ | `--depth` |
| 含 --dry-run 选项 | ✅ | `--dry-run` |
| 含 --recent 选项 | ✅ | `--recent` |

**实际输出（前 500 字符）**
```
usage: mulan codemap [-h] [--depth DEPTH] [--recent RECENT] [--dry-run]

optional arguments:
  -h, --help       show this help message and exit
  --depth DEPTH    目录展开深度（默认 3）
  --recent RECENT  附加最近修改文件数量（默认 0）
  --dry-run        只打印，不写文件
```

#### ✅ [A-02] codemap --dry-run 打印内容不修改文件

**命令**：`mulan codemap --dry-run`  
**期望 exit**：`0`　**实际 exit**：`0`

| 检查项 | 结果 | 期望值 |
|--------|------|--------|
| exit code 为 0 | ✅ | `0` |
| stdout 有内容 | ✅ | `>0 chars` |
| 文件未被修改（mtime 不变） | ✅ | `mtime 不变` |

**实际输出（前 500 字符）**
```
# Codemap — 项目目录快照

> **自动生成** · 2026-04-29 11:54 UTC · 勿手动编辑
> 使用 `python3 scripts/mms/codemap.py` 刷新

---

## 后端应用层 (`backend/app`)

_目录不存在_

## 前端源码 (`frontend/src`)

_目录不存在_

## MMS 记忆系统脚本 (`scripts/mms`)

_目录不存在_

## 共享记忆库 (`docs/memory/shared`)

_目录不存在_

## 架构文档 (`docs/architecture`)

_目录不存在_

---

_本文件由 `scripts/mms/codemap.py` 自动生成，请勿手动编辑。刷新命令：`python3 scripts/mms/cli.py codemap`_
```

#### ✅ [A-03] codemap 无参数运行 exit=0，生成文件

**命令**：`mulan codemap`  
**期望 exit**：`0`　**实际 exit**：`0`

| 检查项 | 结果 | 期望值 |
|--------|------|--------|
| exit code 为 0 | ✅ | `0` |
| 含 已生成 或 codemap | ✅ | `已生成/codemap` |
| codemap.md 文件存在 | ✅ | `文件存在` |

**实际输出（前 500 字符）**
```
✅ codemap 已生成：docs/memory/_system/codemap.md
  · 目录深度: 3 层 | 字符数: 392
```

#### ✅ [A-04] codemap.md 含合法 Markdown 标题结构

**命令**：`（读取 codemap.md 验证）`  
**期望 exit**：`0`　**实际 exit**：`0`

| 检查项 | 结果 | 期望值 |
|--------|------|--------|
| 含 # 标题 | ✅ | `# 标题` |
| 文件非空 | ✅ | `>0 chars` |

**实际输出（前 500 字符）**
```
# Codemap — 项目目录快照

> **自动生成** · 2026-04-29 11:54 UTC · 勿手动编辑
> 使用 `python3 scripts/mms/codemap.py` 刷新

---

## 后端应用层 (`backend/app`)

_目录不存在_

## 前端源码 (`frontend/src`)

_目录不存在_

## MMS 记忆系统脚本 (`scrip
```

#### ✅ [A-05] codemap.md 含自动生成标记（勿手动编辑）

**命令**：`（读取 codemap.md 验证）`  
**期望 exit**：`0`　**实际 exit**：`0`

| 检查项 | 结果 | 期望值 |
|--------|------|--------|
| 含自动生成说明 | ✅ | `自动生成/勿手动` |

**实际输出（前 500 字符）**
```
# Codemap — 项目目录快照

> **自动生成** · 2026-04-29 11:54 UTC · 勿手动编辑
> 使用 `python3 scripts/mms/codemap.py` 刷新

---

## 后端应用层 (`backend/app`)

_目录不存在_

## 前端源码 (`frontend/src`)

_目录不存在_

## MMS 记忆系统脚本 (`scrip
```

#### ✅ [A-06] codemap 重复运行幂等（第二次 exit=0）

**命令**：`mulan codemap（第二次）`  
**期望 exit**：`0`　**实际 exit**：`0`

| 检查项 | 结果 | 期望值 |
|--------|------|--------|
| exit code 为 0 | ✅ | `0` |
| 文件仍存在 | ✅ | `文件存在` |

**实际输出（前 500 字符）**
```
✅ codemap 已生成：docs/memory/_system/codemap.md
  · 目录深度: 3 层 | 字符数: 392
```

#### ✅ [A-07] codemap --depth 2 参数被接受

**命令**：`mulan codemap --depth 2 --dry-run`  
**期望 exit**：`0`　**实际 exit**：`0`

| 检查项 | 结果 | 期望值 |
|--------|------|--------|
| exit code 为 0 | ✅ | `0` |
| 不含 Traceback | ✅ | `无 Traceback` |

**实际输出（前 500 字符）**
```
# Codemap — 项目目录快照

> **自动生成** · 2026-04-29 11:54 UTC · 勿手动编辑
> 使用 `python3 scripts/mms/codemap.py` 刷新

---

## 后端应用层 (`backend/app`)

_目录不存在_

## 前端源码 (`frontend/src`)

_目录不存在_

## MMS 记忆系统脚本 (`scripts/mms`)

_目录不存在_

## 共享记忆库 (`docs/memory/shared`)

_目录不存在_

## 架构文档 (`docs/architecture`)

_目录不存在_

---

_本文件由 `scripts/mms/codemap.py` 自动生成，请勿手动编辑。刷新命令：`python3 scripts/mms/cli.py codemap`_
```

#### ✅ [A-08] codemap 运行完成 < 30 秒

**命令**：`mulan codemap（计时）`  
**期望 exit**：`0`　**实际 exit**：`0`

| 检查项 | 结果 | 期望值 |
|--------|------|--------|
| exit code 为 0 | ✅ | `0` |
| 耗时 0.06s < 30s | ✅ | `<30s` |

### mulan funcmap 组（B 组）

#### ✅ [B-01] funcmap --help 正常显示

**命令**：`mulan funcmap --help`  
**期望 exit**：`0`　**实际 exit**：`0`

| 检查项 | 结果 | 期望值 |
|--------|------|--------|
| exit code 为 0 | ✅ | `0` |
| 含 --backend-only 选项 | ✅ | `--backend-only` |
| 含 --dry-run 选项 | ✅ | `--dry-run` |

**实际输出（前 500 字符）**
```
usage: mulan funcmap [-h] [--backend-only] [--frontend-only] [--dry-run]

optional arguments:
  -h, --help       show this help message and exit
  --backend-only   只扫描后端 Python
  --frontend-only  只扫描前端 TS/TSX
  --dry-run        只打印，不写文件
```

#### ✅ [B-02] funcmap --dry-run 打印内容不修改文件

**命令**：`mulan funcmap --dry-run`  
**期望 exit**：`0`　**实际 exit**：`0`

| 检查项 | 结果 | 期望值 |
|--------|------|--------|
| exit code 为 0 | ✅ | `0` |
| stdout 有内容 | ✅ | `>0 chars` |
| 文件未被修改（mtime 不变） | ✅ | `mtime 不变` |

**实际输出（前 500 字符）**
```
# Funcmap — 函数签名索引

> **自动生成** · 2026-04-29 11:54 UTC · 勿手动编辑
> 仅包含有 docstring/JSDoc 注释的公开函数
> 使用 `python3 scripts/mms/funcmap.py` 刷新

---

## 后端函数索引

## 前端函数索引

---

_本文件由 `scripts/mms/funcmap.py` 自动生成。刷新命令：`python3 scripts/mms/cli.py funcmap`_
```

#### ✅ [B-03] funcmap 无参数运行 exit=0，生成文件

**命令**：`mulan funcmap`  
**期望 exit**：`0`　**实际 exit**：`0`

| 检查项 | 结果 | 期望值 |
|--------|------|--------|
| exit code 为 0 | ✅ | `0` |
| 含 已生成 或 funcmap | ✅ | `已生成/funcmap` |
| funcmap.md 文件存在 | ✅ | `文件存在` |

**实际输出（前 500 字符）**
```
✅ funcmap 已生成：docs/memory/_system/funcmap.md
  · 字符数: 246
```

#### ✅ [B-04] funcmap.md 含合法 Markdown 结构

**命令**：`（读取 funcmap.md 验证）`  
**期望 exit**：`0`　**实际 exit**：`0`

| 检查项 | 结果 | 期望值 |
|--------|------|--------|
| 含 # 标题 | ✅ | `# 标题` |
| 文件非空 | ✅ | `>0 chars` |

**实际输出（前 500 字符）**
```
# Funcmap — 函数签名索引

> **自动生成** · 2026-04-29 11:54 UTC · 勿手动编辑
> 仅包含有 docstring/JSDoc 注释的公开函数
> 使用 `python3 scripts/mms/funcmap.py` 刷新

---

## 后端函数索引

## 前端函数索引

---

_本文件由 `scripts/mms/funcmap.py` 自动
```

#### ✅ [B-05] funcmap.md 含自动生成标记

**命令**：`（读取 funcmap.md 验证）`  
**期望 exit**：`0`　**实际 exit**：`0`

| 检查项 | 结果 | 期望值 |
|--------|------|--------|
| 含自动生成说明 | ✅ | `自动生成/勿手动` |

**实际输出（前 500 字符）**
```
# Funcmap — 函数签名索引

> **自动生成** · 2026-04-29 11:54 UTC · 勿手动编辑
> 仅包含有 docstring/JSDoc 注释的公开函数
> 使用 `python3 scripts/mms/funcmap.py` 刷新

---

## 后端函数索引

## 前端函数索引

---

_本文件由 `scripts/mms/funcmap.py` 自动
```

#### ✅ [B-06] funcmap 重复运行幂等（第二次 exit=0）

**命令**：`mulan funcmap（第二次）`  
**期望 exit**：`0`　**实际 exit**：`0`

| 检查项 | 结果 | 期望值 |
|--------|------|--------|
| exit code 为 0 | ✅ | `0` |
| 文件仍存在 | ✅ | `文件存在` |

**实际输出（前 500 字符）**
```
✅ funcmap 已生成：docs/memory/_system/funcmap.md
  · 字符数: 246
```

#### ✅ [B-07] funcmap --backend-only --dry-run 正常运行

**命令**：`mulan funcmap --backend-only --dry-run`  
**期望 exit**：`0`　**实际 exit**：`0`

| 检查项 | 结果 | 期望值 |
|--------|------|--------|
| exit code 为 0 | ✅ | `0` |
| 不含 Traceback | ✅ | `无 Traceback` |

**实际输出（前 500 字符）**
```
# Funcmap — 函数签名索引

> **自动生成** · 2026-04-29 11:54 UTC · 勿手动编辑
> 仅包含有 docstring/JSDoc 注释的公开函数
> 使用 `python3 scripts/mms/funcmap.py` 刷新

---

## 后端函数索引

---

_本文件由 `scripts/mms/funcmap.py` 自动生成。刷新命令：`python3 scripts/mms/cli.py funcmap`_
```

#### ✅ [B-08] funcmap 运行完成 < 60 秒

**命令**：`mulan funcmap（计时）`  
**期望 exit**：`0`　**实际 exit**：`0`

| 检查项 | 结果 | 期望值 |
|--------|------|--------|
| exit code 为 0 | ✅ | `0` |
| 耗时 0.07s < 60s | ✅ | `<60s` |

### mulan ast-diff 组（C 组）

#### ✅ [C-01] ast-diff --help 正常显示

**命令**：`mulan ast-diff --help`  
**期望 exit**：`0`　**实际 exit**：`0`

| 检查项 | 结果 | 期望值 |
|--------|------|--------|
| exit code 为 0 | ✅ | `0` |
| 含 --ep 选项 | ✅ | `--ep` |
| 含 --before 选项 | ✅ | `--before` |
| 含 --after 选项 | ✅ | `--after` |

**实际输出（前 500 字符）**
```
usage: mulan ast-diff [-h] [--ep EP-NNN] [--before FILE] [--after FILE]
                      [--files FILE [FILE ...]]

optional arguments:
  -h, --help            show this help message and exit
  --ep EP-NNN           比对此 EP 的 precheck 快照 vs 当前状态
  --before FILE         比对前的 ast_index.json 路径
  --after FILE          比对后的 ast_index.json 路径（默认：当前 ast_index.json）
  --files FILE [FILE ...]
                        只比对这些文件（空则全量比对）
```

#### ✅ [C-02] ast-diff 无参数 exit=1 + 友好提示

**命令**：`mulan ast-diff`  
**期望 exit**：`1`　**实际 exit**：`1`

| 检查项 | 结果 | 期望值 |
|--------|------|--------|
| exit code 为 1 | ✅ | `1` |
| 含 请指定 或 --ep 或 --before | ✅ | `--ep/--before/请指定` |

**实际输出（前 500 字符）**
```
❌ 请指定 --ep 或 --before
```

#### ✅ [C-03] ast-diff --before/--after 比对 JSON 差异

**命令**：`mulan ast-diff --before ci_ast_before.json --after ci_ast_after.json`  
**期望 exit**：`0`　**实际 exit**：`0`

| 检查项 | 结果 | 期望值 |
|--------|------|--------|
| exit code 不为 -1（不超时崩溃） | ✅ | `≠ -1` |
| 不含 Traceback | ✅ | `无 Traceback` |
| 输出含差异或无变更提示 | ✅ | `>0 输出/exit=0` |

**实际输出（前 500 字符）**
```
AST 契约 Diff：_ci_ast_before.json → _ci_ast_after.json
无契约变更（比对 1 个文件）
```

#### ✅ [C-04] ast-diff 同一文件比对（无变更）

**命令**：`mulan ast-diff --before ci_ast_A.json --after ci_ast_A.json`  
**期望 exit**：`0`　**实际 exit**：`0`

| 检查项 | 结果 | 期望值 |
|--------|------|--------|
| exit code 不为 -1 | ✅ | `≠ -1` |
| 不含 Traceback | ✅ | `无 Traceback` |
| 输出含 无变更 或 0 条变更 或为空（表示无差异） | ✅ | `无变更/0/空输出/exit=0` |

**实际输出（前 500 字符）**
```
AST 契约 Diff：_ci_ast_before.json → _ci_ast_before.json
无契约变更（比对 1 个文件）
```

#### ✅ [C-05] ast-diff --before 不存在文件 exit=1

**命令**：`mulan ast-diff --before /tmp/__ci_nonexistent__.json ...`  
**期望 exit**：`1`　**实际 exit**：`1`

| 检查项 | 结果 | 期望值 |
|--------|------|--------|
| exit code 为 1 | ✅ | `1` |
| 含错误提示 | ✅ | `❌/不存在/找不到` |

**实际输出（前 500 字符）**
```
❌ before 快照不存在：/tmp/__ci_nonexistent_ast__.json
```

#### ✅ [C-06] ast-diff --files 过滤文件范围

**命令**：`mulan ast-diff --before A --after B --files cli.py`  
**期望 exit**：`0`　**实际 exit**：`0`

| 检查项 | 结果 | 期望值 |
|--------|------|--------|
| exit code 不为 -1 | ✅ | `≠ -1` |
| 不含 Traceback | ✅ | `无 Traceback` |

**实际输出（前 500 字符）**
```
AST 契约 Diff：_ci_ast_before.json → _ci_ast_after.json
范围：['cli.py']
无契约变更（比对 1 个文件）
```
